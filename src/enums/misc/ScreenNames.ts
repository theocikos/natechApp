export enum ScreenNames {
  // Root level
  NOT_FOUND = "not-found",
  INDEX = "index",

  /* (without-session) */
  WELCOME = "welcome",
  SIGNIN = "signIn",

  /* (with-session) */
  HOME = "home",
  SETTINGS = "settings",
  SEND_MONEY = "send-money",
  REVIEW_TRANSACTION = "review-transaction",
}